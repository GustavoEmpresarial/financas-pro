// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: alt_investments.sql

package gen

import (
	"context"
)

const getAltInvestment = `-- name: GetAltInvestment :one
SELECT id, user_id, name, type, invested_amount, current_value, purchase_date, maturity_date, expected_return, risk_level, platform, notes, logo_url, is_active, deleted_at, created_at, updated_at FROM alt_investments
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type GetAltInvestmentParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) GetAltInvestment(ctx context.Context, arg GetAltInvestmentParams) (AltInvestment, error) {
	row := q.db.QueryRow(ctx, getAltInvestment, arg.ID, arg.UserID)
	var i AltInvestment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Type,
		&i.InvestedAmount,
		&i.CurrentValue,
		&i.PurchaseDate,
		&i.MaturityDate,
		&i.ExpectedReturn,
		&i.RiskLevel,
		&i.Platform,
		&i.Notes,
		&i.LogoUrl,
		&i.IsActive,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listAltInvestments = `-- name: ListAltInvestments :many

SELECT id, user_id, name, type, invested_amount, current_value, purchase_date, maturity_date, expected_return, risk_level, platform, notes, logo_url, is_active, deleted_at, created_at, updated_at FROM alt_investments
WHERE user_id = $1 AND deleted_at IS NULL
ORDER BY created_at DESC
`

// Queries de alt_investments. ORDER BY replica o orderBy do modulo legado
// correspondente: mudar a ordem muda a tela sem ninguem pedir.
func (q *Queries) ListAltInvestments(ctx context.Context, userID string) ([]AltInvestment, error) {
	rows, err := q.db.Query(ctx, listAltInvestments, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AltInvestment{}
	for rows.Next() {
		var i AltInvestment
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Type,
			&i.InvestedAmount,
			&i.CurrentValue,
			&i.PurchaseDate,
			&i.MaturityDate,
			&i.ExpectedReturn,
			&i.RiskLevel,
			&i.Platform,
			&i.Notes,
			&i.LogoUrl,
			&i.IsActive,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteAltInvestment = `-- name: SoftDeleteAltInvestment :execrows
UPDATE alt_investments
SET deleted_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type SoftDeleteAltInvestmentParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) SoftDeleteAltInvestment(ctx context.Context, arg SoftDeleteAltInvestmentParams) (int64, error) {
	result, err := q.db.Exec(ctx, softDeleteAltInvestment, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
