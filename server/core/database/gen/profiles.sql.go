// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: profiles.sql

package gen

import (
	"context"
)

const createProfile = `-- name: CreateProfile :one
INSERT INTO profiles (id, user_id, display_name, theme_preference)
VALUES ($1, $2, $3, $4)
RETURNING id, user_id, display_name, avatar_url, theme_preference, created_at, updated_at
`

type CreateProfileParams struct {
	ID              string  `json:"id"`
	UserID          string  `json:"userId"`
	DisplayName     *string `json:"displayName"`
	ThemePreference *string `json:"themePreference"`
}

func (q *Queries) CreateProfile(ctx context.Context, arg CreateProfileParams) (Profile, error) {
	row := q.db.QueryRow(ctx, createProfile,
		arg.ID,
		arg.UserID,
		arg.DisplayName,
		arg.ThemePreference,
	)
	var i Profile
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DisplayName,
		&i.AvatarUrl,
		&i.ThemePreference,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getProfileByUserID = `-- name: GetProfileByUserID :one
SELECT id, user_id, display_name, avatar_url, theme_preference, created_at, updated_at FROM profiles WHERE user_id = $1
`

func (q *Queries) GetProfileByUserID(ctx context.Context, userID string) (Profile, error) {
	row := q.db.QueryRow(ctx, getProfileByUserID, userID)
	var i Profile
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DisplayName,
		&i.AvatarUrl,
		&i.ThemePreference,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateProfile = `-- name: UpdateProfile :one
UPDATE profiles SET
    display_name     = COALESCE($1, display_name),
    avatar_url       = COALESCE($2, avatar_url),
    theme_preference = COALESCE($3, theme_preference),
    updated_at       = CURRENT_TIMESTAMP
WHERE user_id = $4
RETURNING id, user_id, display_name, avatar_url, theme_preference, created_at, updated_at
`

type UpdateProfileParams struct {
	DisplayName     *string `json:"displayName"`
	AvatarUrl       *string `json:"avatarUrl"`
	ThemePreference *string `json:"themePreference"`
	UserID          string  `json:"userId"`
}

// COALESCE deixa o update ser parcial: parametro nulo mantem a coluna como
// esta. E o equivalente do `data: { ...campos enviados }` do Prisma.
func (q *Queries) UpdateProfile(ctx context.Context, arg UpdateProfileParams) (Profile, error) {
	row := q.db.QueryRow(ctx, updateProfile,
		arg.DisplayName,
		arg.AvatarUrl,
		arg.ThemePreference,
		arg.UserID,
	)
	var i Profile
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DisplayName,
		&i.AvatarUrl,
		&i.ThemePreference,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
