// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: goals.sql

package gen

import (
	"context"
)

const getGoal = `-- name: GetGoal :one
SELECT id, user_id, name, target_amount, current_amount, deadline, category, priority, status, monthly_target, color, notes, deleted_at, created_at, updated_at FROM financial_goals
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type GetGoalParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) GetGoal(ctx context.Context, arg GetGoalParams) (FinancialGoal, error) {
	row := q.db.QueryRow(ctx, getGoal, arg.ID, arg.UserID)
	var i FinancialGoal
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.TargetAmount,
		&i.CurrentAmount,
		&i.Deadline,
		&i.Category,
		&i.Priority,
		&i.Status,
		&i.MonthlyTarget,
		&i.Color,
		&i.Notes,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listGoals = `-- name: ListGoals :many

SELECT id, user_id, name, target_amount, current_amount, deadline, category, priority, status, monthly_target, color, notes, deleted_at, created_at, updated_at FROM financial_goals
WHERE user_id = $1 AND deleted_at IS NULL
ORDER BY created_at DESC
`

// Queries de financial_goals. ORDER BY replica o orderBy do modulo legado
// correspondente: mudar a ordem muda a tela sem ninguem pedir.
func (q *Queries) ListGoals(ctx context.Context, userID string) ([]FinancialGoal, error) {
	rows, err := q.db.Query(ctx, listGoals, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FinancialGoal{}
	for rows.Next() {
		var i FinancialGoal
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.TargetAmount,
			&i.CurrentAmount,
			&i.Deadline,
			&i.Category,
			&i.Priority,
			&i.Status,
			&i.MonthlyTarget,
			&i.Color,
			&i.Notes,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteGoal = `-- name: SoftDeleteGoal :execrows
UPDATE financial_goals
SET deleted_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type SoftDeleteGoalParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) SoftDeleteGoal(ctx context.Context, arg SoftDeleteGoalParams) (int64, error) {
	result, err := q.db.Exec(ctx, softDeleteGoal, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
