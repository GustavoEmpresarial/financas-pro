// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: audit.sql

package gen

import (
	"context"
)

const listAudits = `-- name: ListAudits :many
SELECT id, table_name, record_id, action, old_data, new_data, user_id, created_at FROM record_audits
WHERE table_name = $1 AND record_id = $2 AND user_id = $3
ORDER BY created_at DESC
LIMIT 100
`

type ListAuditsParams struct {
	TableName string  `json:"tableName"`
	RecordID  string  `json:"recordId"`
	UserID    *string `json:"userId"`
}

// O legado NAO filtrava por dono aqui: bastava saber tabela e id para ler o
// historico de qualquer registro, de qualquer usuario. O filtro por user_id
// fecha isso. A tabela esta sempre vazia hoje (nada escreve auditoria), entao
// na pratica nenhuma tela muda de comportamento.
func (q *Queries) ListAudits(ctx context.Context, arg ListAuditsParams) ([]RecordAudit, error) {
	rows, err := q.db.Query(ctx, listAudits, arg.TableName, arg.RecordID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []RecordAudit{}
	for rows.Next() {
		var i RecordAudit
		if err := rows.Scan(
			&i.ID,
			&i.TableName,
			&i.RecordID,
			&i.Action,
			&i.OldData,
			&i.NewData,
			&i.UserID,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
