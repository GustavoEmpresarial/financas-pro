// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: credit_cards.sql

package gen

import (
	"context"
)

const getCreditCard = `-- name: GetCreditCard :one
SELECT id, user_id, name, brand, total_limit, closing_day, due_day, color, card_type, image_url, card_network, is_active, notes, deleted_at, created_at, updated_at FROM credit_cards
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type GetCreditCardParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) GetCreditCard(ctx context.Context, arg GetCreditCardParams) (CreditCard, error) {
	row := q.db.QueryRow(ctx, getCreditCard, arg.ID, arg.UserID)
	var i CreditCard
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Brand,
		&i.TotalLimit,
		&i.ClosingDay,
		&i.DueDay,
		&i.Color,
		&i.CardType,
		&i.ImageUrl,
		&i.CardNetwork,
		&i.IsActive,
		&i.Notes,
		&i.DeletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listCreditCards = `-- name: ListCreditCards :many

SELECT id, user_id, name, brand, total_limit, closing_day, due_day, color, card_type, image_url, card_network, is_active, notes, deleted_at, created_at, updated_at FROM credit_cards
WHERE user_id = $1 AND deleted_at IS NULL
ORDER BY name ASC
`

// Queries de credit_cards. ORDER BY replica o orderBy do modulo legado
// correspondente: mudar a ordem muda a tela sem ninguem pedir.
func (q *Queries) ListCreditCards(ctx context.Context, userID string) ([]CreditCard, error) {
	rows, err := q.db.Query(ctx, listCreditCards, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CreditCard{}
	for rows.Next() {
		var i CreditCard
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Brand,
			&i.TotalLimit,
			&i.ClosingDay,
			&i.DueDay,
			&i.Color,
			&i.CardType,
			&i.ImageUrl,
			&i.CardNetwork,
			&i.IsActive,
			&i.Notes,
			&i.DeletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteCreditCard = `-- name: SoftDeleteCreditCard :execrows
UPDATE credit_cards
SET deleted_at = CURRENT_TIMESTAMP, updated_at = CURRENT_TIMESTAMP
WHERE id = $1 AND user_id = $2 AND deleted_at IS NULL
`

type SoftDeleteCreditCardParams struct {
	ID     string `json:"id"`
	UserID string `json:"userId"`
}

func (q *Queries) SoftDeleteCreditCard(ctx context.Context, arg SoftDeleteCreditCardParams) (int64, error) {
	result, err := q.db.Exec(ctx, softDeleteCreditCard, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
