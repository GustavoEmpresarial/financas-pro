# docs

- `architecture/` — visão geral, camadas, fluxo de um request
- `modules/` — um arquivo por módulo do backend + `_template.md` com a forma padrão
- `api/` — o contrato HTTP, rota por rota
- `decisions/` — ADRs numerados: o que foi decidido, por quê, e o que reverteria
