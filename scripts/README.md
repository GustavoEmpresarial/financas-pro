# scripts

- `database/` — baseline do goose, backup, restore, seed
- `maintenance/` — utilitários de operação (ex.: `contract-diff.sh`, que compara
  as respostas do backend Go com as do legado Fastify)
- `deploy/` — RESERVADO. Build e publicação. Vazio enquanto o deploy for
  `docker compose up` na mão.
